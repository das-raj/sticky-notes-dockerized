version: '3'

services:
  db:
    image: mysql:5.7
    ports:
      - '3306:3306'
    environment:
      MYSQL_DATABASE: 'stickynotes'
      MYSQL_PASSWORD: 'palakraj22'
      MYSQL_ROOT_PASSWORD: 'palakraj22'
    expose:
      - '3306'
  web:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - '8000:8000'
    depends_on:
      - db